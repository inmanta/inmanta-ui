description: Removed the dedicated /console/version.json route and its test references. The web-console no longer generates version.json (it dropped the update banner that used it), and the file, when present, is already served by the general static-file handler, so the special route was redundant.
issue-nr: 6482
change-type: patch
destination-branches: [master, iso9]
sections:
  minor-improvement: "{{description}}"
